<h1>Dashboard</h1>
<div class="container bg-secondary-subtle">
  <% @deeds.each do |deed| %>
    <% if deed.user == current_user %>
      <%= render 'shared/deed_cards', deed: deed %>
    <% end %>
  <% end %>
</div>
