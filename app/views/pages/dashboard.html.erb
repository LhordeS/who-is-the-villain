<h1 class="dashboard-title">Dashboard</h1>
<div class="container card-container">
  <% @deeds.each do |deed| %>
    <% if deed.user == current_user %>
      <%= render 'shared/deed_cards', deed: deed %>
    <% end %>
  <% end %>
</div>
