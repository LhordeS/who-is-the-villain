<div id="messages">
  <% deed.messages.each do |message| %>
  <%= render "messages/message", message: message %>
<% end %>
</div>

<div id="new_message_container">
  <%= render "messages/form", deed: deed, message: message %>
</div>
